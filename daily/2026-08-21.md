---
tags: [daily, portfolio-generator, lint, readme-parsing]
created: 2026-08-21
updated: 2026-08-21
---

# 2026-08-21

## 🎯 Sessão — portfolio-generator: verificação de estado + auditoria de qualidade das descrições

### ✅ Concluído

**Verificação de estado do graphify** (`graphify-out/`): grafo atualizado, `commit`
idêntico ao `HEAD` (`f8e79802`), sem `graphify update` necessário. `graphify-out/`
adicionado ao `.gitignore` (artefato regenerável).

**Correção de lint/type quebrados** (TODO.md dizia "✅" mas estava desatualizado):
- mypy: 2 erros reais em `src/cli/main.py` (sort key do `report` acessando `.get()` em
  `object` não tipado) — corrigido com `_nested_str`/`_nested_int` + `isinstance`, sem
  `type: ignore`.
- ruff: 140 violações de estilo corrigidas em `src/` e `scripts/` (`List`/`Tuple` →
  `list`/`tuple`, `raise...from`, comprehensions).
- 247 testes passando, 96% cobertura mantida.

**Plano em fases para revisão de descrições** (aprovado via plan mode), executado:
1. Causa raiz corrigida: `extract_description()` (`src/analyzer/readme_parser.py`) agora
   filtra badges/shields markdown, blocos de código e cabeçalhos de metadados antes de
   extrair o primeiro parágrafo — 7 testes novos.
2. Confirmado que `description_source="ai-generated"` já era o valor correto no código;
   o output antigo (`"generated"`) só estava desatualizado.
3. Auditoria das 55 descrições reais do scan de 2026-06-18 (`output/portfolio-2026-06-18.json`)
   cruzando com disponibilidade de `graphify-out/` por projeto (22/55 tinham).
4. Descoberto que **3 projetos não existem mais no disco** (`Cognitive-Speech-TTS`,
   `knowledge-lab`, `pmm-dev`) — removidos do portfólio revisado.
5. Regeneração: 5 descrições reescritas por mim (Claude, sem API Anthropic — sem
   `.secrets/api_config.json`), 7 só normalizadas no `description_source` (descrição já
   era boa).
6. `output/portfolio-2026-06-18-revisado.json` gerado e validado contra o schema.

**Commit + PR** (INFRA-02 do TODO — CI nunca tinha rodado nesta branch):
- `f77a292` — fix lint/type + readme_parser + relatório da auditoria.
- PR #7 aberto: https://github.com/yvesmarinho/portfolio-generator/pull/7

**Scan real** (`portfolio-gen scan --no-ai`), confirmando a auditoria manual: 52 → 46
projetos após:
- Exclusões adicionadas em `config/scan-exclusions.json` a pedido do usuário: `arduino`,
  `domotica`, `wifi-qrcode-generator`, `yvesmarinho_git_profile`, `enterprise-insurance`,
  `enterprise-kubernets-operation`.
- `README.md` criado em `modelos_variados/` (projeto sem descrição própria).
- 3 descrições finais (`code-snippet`, `enterprise-docker`, `enterprise-observability`)
  escritas por mim porque o README genuinamente não tinha parágrafo descritivo.
- `output/portfolio-2026-08-21.json`: 46 projetos, 0 descrições ruins, válido.
- Commit `bc598da` (portfolio-generator, exclusões) + commit `cbb99da` (modelos_variados,
  README — **commitado direto em `main`** por decisão explícita do usuário, exceção aceita
  a um repositório pessoal sem PR/CI).

### 🔍 Descobertas técnicas

- `mcp-obsidian` estava resolvendo para um vault **diferente** (pessoal, `0-Projetos/`,
  `5-Diário/`) — a memória do Claude Code fica em `~/DevOps/claude_memory/claude_memory/`
  (filesystem direto), não no vault pessoal do usuário. Corrigido pelo usuário durante a
  sessão.
- Alguns projetos "com descrição ruim" na amostra real não eram bug do parser — eram
  README genuinamente sem conteúdo útil (`code-snippet`: "Projeto concluído."), ou o
  README antigo já era bom mas com `description_source` desatualizado (`"generated"` em
  vez de `"ai-generated"`) — regenerar cegamente teria piorado a qualidade em alguns casos
  (ex.: `enterprise-kubernets-operation` mencionava Traefik/Bookstack especificamente,
  minha primeira tentativa de reescrita era mais vaga). Corrigido comparando antes/depois
  e só regerando o que era realmente ruído.
- `report --format table/csv` não expõe `description` — só `--format json`.

### 📌 Pendente / Próximos passos

- [ ] Configurar `.secrets/api_config.json` se quiser que o `ai_summarizer` real (via API
      Anthropic) gere descrições, em vez de eu escrever manualmente.
- [ ] Corrigir `README_NAMES` em `readme_parser.py` para aceitar variações comuns de nome
      (`READE.md`, `CLUSTER_README.md`, `START_HERE.md`).
- [ ] Filtrar linha `---` (separador horizontal) em `extract_description()`.
- [ ] `report --show-description` ou coluna de descrição no `--format table`.
- [ ] Revisar se `output/portfolio-2026-08-21.json` deve virar o novo "canônico" do
      projeto (está gitignorado, então não é versionado automaticamente).

### 🔗 Arquivos modificados

**portfolio-generator** (branch `001-portfolio-gen-spec`):
- `.gitignore`, `src/cli/main.py`, `src/analyzer/readme_parser.py`,
  `tests/unit/test_readme_parser.py`, `config/scan-exclusions.json`,
  `scripts/lib/*.py`, `scripts/mem_*.py`, `scripts/session-*.py` (ruff --fix)
- `docs/SESSIONS/2026-08-21/RELATORIO_REVISAO_DESCRICOES_2026-08-21.md` (novo)
- Commits: `f77a292`, `bc598da` — PR #7

**modelos_variados** (branch `main`):
- `README.md` (novo) — commit `cbb99da`

### 🏷️ Links relacionados

- [[../projects/portfolio-generator|portfolio-generator]] (nova nota de projeto criada
  nesta sessão)
