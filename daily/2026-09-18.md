---
tags: [daily, praxisforge, obsidian]
created: 2026-09-18
updated: 2026-09-18
---

# 2026-09-18

## 🎯 Sessão — praxisforge: definição do objetivo-init e integração com Obsidian

### ✅ Concluído

- Atualizado `objetivo-init-praxisforge.md`: rastreamento de sessão passa a usar o vault `claude_memory` (pasta `daily/`), dados do projeto em `projects/`; removida a referência a `docs/SESSIONS/` (seção de regras, `folder_structure` e `work_style`).
- Criada a nota do projeto [[praxisforge]] em `projects/`.
- Criada esta nota diária e atualizado o [[00-index]] com ambas.

### 🧭 Decisões

- Toda memória/documentação persistente do PraxisForge usa o Obsidian (`daily/` para sessões, `projects/` para o projeto); todo arquivo novo no vault deve entrar no `00-index.md` com descrição.
- Biblioteca de skills mantida no repo (`skills/`, versionada em git); vault só com catálogo em [[praxisforge]] e templates de criação. Publicação em outros projetos por `scripts/publish-skills` → `~/.claude/skills/` ou `.claude/skills/`. Motivo: vault não tem versionamento/CI e cópias duplicadas geram drift.
- Fontes a curar: registro em `src/data/sources/` no repo (versionado); material bruto em `~/DevOps/github_forks` (fora do repo, caminho via config). Licença validada por fonte.
- Requisito: manter base de dados das pastas com informações a curar; armazenamento decidido: YAML versionado no repo (SQLite descartado: diff ruim no git, poucas pastas).
- Revisão da spec (20 problemas): corrigidos `BaseException`, Python 3.12+, `ruff format`, ADRs em `docs/decisions/`, pre-commit/GitGuardian, ordem TDD, referências quebradas e tabela de pendências; Fase 2 reescrita para cobrir o pipeline completo e Fase 3 ampliada (ponta a ponta, idempotência, critério de 95%).
- Registros movidos para `src/data` (`folders.yaml` e `sources/`); alias de caminho via config/env, licença por fonte, `schema_version` em JSON/YAML/frontmatter.
- Arquitetura: camadas + casos de uso com Presentation; `.memory` removido; ciclo de curadoria mensal; dependências-chave definidas; critério de sucesso ≥ 95% de aprovação na validação.
- Branch principal definida como `main`: branch local renomeada de `master` e scaffold rebaseado sobre o `origin/main` (commit inicial do GitHub, históricos eram independentes). Push pendente, via branch + PR.
- Objetivo-init declarado versão final; PR #1 aberto (`docs-objetivo-init-praxisforge` → `main`, 2 commits + scaffold).
- Workflow speckit iniciado: constituição v1.0.0 criada em `.specify/memory/constitution.md` (7 princípios: camadas, contratos versionados, test-first, erros semânticos, proveniência/licença, skills no repo, memória no vault). Ratificada em 2026-09-18.

### ⏭️ Encerramento / próxima sessão

- Estado: constituição escrita e ainda **sem commit** (arquivo modificado na branch `docs-objetivo-init-praxisforge`); PR #1 aguardando merge.
- Próximo passo speckit: `/speckit-specify` (primeira feature da Fase 2: estrutura em camadas, schemas e registro de pastas), depois plan → tasks → implement.
- Ver [[praxisforge]] seção "Próxima sessão".

### 🔗 Arquivos modificados

- `praxisforge/objetivo-init-praxisforge.md` (ainda não versionado)
- vault: `projects/praxisforge.md`, `daily/2026-09-18.md`, `00-index.md`
