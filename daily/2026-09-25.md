---
tags: [daily, praxisforge]
created: 2026-09-25
updated: 2026-09-25
---

<!-- Criado em: 25/09/2026 09:59 -->
<!-- Modificado em: 25/09/2026 15:19 -->

# 2026-09-25

## 🎯 Sessão — praxisforge: curadoria agent-skills, --description, ruff em scripts/, raiz do projeto

### ✅ Concluído

- PR #20 (skill diretrizes-codificacao) mergeado → main `2b96047`.
- Decisão: `diretrizes-codificacao` **não** publicada no `~/.claude/skills` global (duplicaria o CLAUDE.md global).
- PR #21: curadoria `github_forks__agent_skills` (addyosmani/agent-skills, MIT, commit `d2c37ef`) → fonte `praticas-agentes/agent-skills-constraints` (summary) e skill `guarda-barra-qualidade` 1.0.0; pasta marcada curated.
- PR #22: `folders update --description` (TDD, 9 testes); descrição do karpathy corrigida no registro real.
- PR #23: `scripts/` no gate do ruff (470 → 0; T201 liberado só em scripts/ por decisão do usuário); `ruff format --check src tests scripts` no `make lint`.
- PR #24: CLI acha a raiz pelo `pyproject.toml` do praxisforge subindo do cwd, override `PRAXISFORGE_ROOT`; ADR 0010.

### ⚠️ Observações

- `scripts/session-manager.py` já quebrado na main (`lib.session` inexistente) — registrado no TODO.
- Horários estimados nos cabeçalhos do PR #22 corrigidos com a hora do sistema (commit `fe714da`).
- PRs #21–#24 editam `docs/TODO.md` → esperar conflito de merge nesse arquivo.

## 🎯 Sessão (tarde) — curadoria automatizada: debate, constituição v4 e feature 009

- Debate da curadoria automatizada fechado (`docs/debates/curadoria-automatizada.md`) e dividido em 4 features (009–012).
- PRs #21–#24 mergeados (incidentes: #22 com validador de commit falhando, #24 com checks pendentes; lição registrada).
- Constituição v4.0.0 (PR #25) e feature 009-acervo-library completa (PR #26, CI verde, aguardando merge).

## 🎯 Sessão (fim de tarde) — feature 010-inventario-curadoria

- PR #26 (009) mergeado pelo usuário.
- Ciclo SpecKit completo da 010: specify (Q1 = B: `.md` avulso → `unknown`, resto só no manifesto) → clarify (exclusões só no manifesto; `discarded` = descarte da triagem) → plan → correção do usuário: convenções em `~/.config/praxisforge/curation-conventions.yaml` (repo guarda só o `.example.yaml`) → tasks (40) → analyze (0 críticos; U1/U2 resolvidos com regras de diretório + `marker`) → implement.
- Entregue: `curation inventory (<alias> | --all)` e `curation status [<alias>] [--json]`; manifesto/estado em `~/.config/praxisforge/curation/<alias>/` com `flock` e escrita atômica; reconciliação incremental por hash; dependência `pathspec`; ADR 0013; guia `docs/guides/inventariar-curadoria.md`.
- 1.086 testes, cobertura 97,29%. PR #27 mergeado pelo usuário → `main` em `9e3d89c`.

### ⚠️ Observações

- CI falhou na 1ª rodada: `make lint` roda mypy também em `tests/` (local só tinha rodado `mypy src`) → corrigido em `f2b801c`. Lição: sempre `make lint` antes do push.
- `ruff format .` reformatou bloco de código dentro de `docs/guides/SESSION_DOCS_STYLE_GUIDE.md` — revertido; TODO aberto para excluir docs do formatador.
- Usuário disse ter apagado `.github/agents`, mas as remoções pendentes eram `.claude/commands/speckit.*..md` — commitadas como `f74ad2f` e sinalizado.
- Cabeçalhos `MODIFICADO`/`DATA` de parte dos arquivos da 010 (≈15:05–15:45) foram escritos sem consultar o relógio e ficaram adiantados em relação à hora real (~15:19 no encerramento) — corrigir num commit `chore` na próxima sessão.

### ➡️ Próximos passos

1. Copiar as convenções para `~/.config/praxisforge/`, rodar `curation inventory --all` nas 55 pastas e ampliar as regras a partir dos `unknown`.
2. `/speckit-specify` da 011-triagem-llm.
3. Corrigir os cabeçalhos com horário adiantado.
