---
tags: [daily, praxisforge, buzzclubhub]
created: 2026-09-23
updated: 2026-09-23
---

<!-- Criado em: 23/09/2026 10:20 -->
<!-- Modificado em: 23/09/2026 17:39 -->

# 2026-09-23

## 🎯 Sessão — praxisforge: sincronização do vault com o repositório

### ✅ Concluído

- Contexto carregado do vault; constatado que o vault parava no merge da feature 001 (PR #11).
- Registradas retroativamente em [[praxisforge]] as features 002 (PR #12) e 003 (PR #13), ambas mergeadas em 22/09.
- Estado da `main`: limpa, em `d3bc39d` (merge do PR #13); specs 001/002/003 com todas as tasks concluídas (73/23/40).

### ⏭️ Próxima sessão

- Definir a próxima feature — candidata: detecção de mudança de conteúdo pós-curadoria (hash do HEAD).

## 🎯 Sessão — praxisforge: feature 004-deteccao-mudanca-conteudo (spec → MVP)

### ✅ Concluído

- Fluxo SDD completo: specify → clarify → plan → checklist → tasks → analyze → implement (MVP T001–T023).
- Branch `004-deteccao-mudanca-conteudo`, 4 commits (`9f68731`, `7b37493`, `ed913b0`, `e198614`).
- Entregue: campo opcional `last_curated_commit` (schema v1 aditivo), porta `GitContentInspector` + adapter `GitCliInspector` (executável git, timeout 10 s, `git diff -- .` restrito à pasta), `folders update --status curated` grava o HEAD, `folders show` exibe a versão.
- Métricas: 327 testes, cobertura 96,28%, ruff/mypy/bandit/safety limpos.

### 🧭 Decisões

- Clarificações: legado curado sem hash recebe baseline na 1ª varredura; hash mantido como histórico ao sair de curated; mudança conta só se arquivos **dentro da pasta** mudaram (subpasta de repo maior).
- Git via executável (sem GitPython/pygit2) — zero dependência nova.
- `update_folder()` passou a retornar `UpdateFolderResult(folder, head_recorded)`: achado de bug antes do commit (remarcar curated sem commit novo seria exibido como "não é repositório git").
- Rascunho do schema em `specs/001/contracts/` precisa acompanhar `schemas/` (teste de contrato compara os dois).

### ⚠️ Armadilhas

- `ruff format .` reformata arquivos pré-existentes fora do padrão na `main` (make lint não roda format check) — formatar só arquivos tocados.
- Bandit lê texto após `# nosec` como IDs de teste; justificativa em comentário separado.
- zsh não faz word-splitting de `$VAR` com argumentos.

### ⏭️ Próximo

- T024–T042: US2 (reversão na varredura), US3 (legado), teste de escala, docs, gates, PR.

## 🎯 Sessão — praxisforge: feature 004 concluída (US2, US3, Polish) e PR #14

### ✅ Concluído

- T024–T042: reversão `curated`→`in_curation` na varredura (US2), baseline para legado sem hash (US3), ADR 0005, docs, teste de escala (100 repos < 30 s, ~8 s real).
- Commits `13c694b` (US2), `440d6c9` (US3), `e857e6e` (Polish); PR #14 aberto (`004-deteccao-mudanca-conteudo` → `main`).
- Métricas: 346 testes, cobertura 96,42%; lint, bandit/safety, validate-data e guarda de camadas limpos; quickstart C1–C5 manual ok.

### 🧭 Decisões

- `ContentCheck` (not_applicable, not_git, baseline_recorded, unchanged, reverted) no `ScanResult`; `ScanBatchReport.reverted`.
- Verificação acontece antes de persistir: falha do git não altera status/hash/last_scanned.
- Testes antigos de scan/update mantidos via wrappers com fakes (inspector "não-git" / proibidos), sem reescrever chamadas.

### ⚠️ Armadilhas

- Hook de PreToolUse bloqueia comandos cujo texto contém "não" + hífen (falso positivo de `--no-verify`) — escrever mensagens via arquivo.
- zsh do usuário tem alias `g` e `uv`; usar `whence -p` / scripts bash no scratchpad.

### ⏭️ Próximo

- Acompanhar CI do PR #14 e mergear; depois limpar a branch.

- 23/09/2026 12:28: PR #14 com CI verde e mergeado em `main` (merge `c6d5d59`); branch `004-deteccao-mudanca-conteudo` removida (local e remota).

## 🎯 Sessão — buzzclubhub: validação para atualizar produção (15:24)

- **Objetivo**: recuperar contexto da sessão de 15/09 e validar se há impedimento para atualizar produção (PR #16 `develop` → `main` no upstream).
- **Resultado**: bloqueado — PR em conflito (`package-lock.json`), CI do `develop` vermelho (Test + coverage), 2 migrations fixture local-only e 1 migration de RLS não idempotente no caminho de produção.
- **Status**: aguardando credenciais do banco de dados de produção. Nenhum arquivo/commit alterado no repo.
- Detalhe: [[projects/buzzclubhub]] (Atualização 23/09/2026).

## 🎯 Sessão — praxisforge: feature 005-caminho-absoluto-registro (constituição v2.0.0)

### ✅ Concluído

- Constituição emendada para **v2.0.0** (Princípio V): `folders.yaml` passa a ter caminho absoluto obrigatório por pasta (decisão do usuário após avaliar raiz+relativo e `~/`).
- Fluxo SDD completo: specify (3 clarificações) → clarify (2) → plan → checklist `identidade-pasta` (15 itens; lacunas corrigidas: sufixo para raízes homônimas, sem aninhamento, comparação sem maiúsculas) → tasks (41) → analyze → implement.
- Entregue: `folders-schema-v2` (path obrigatório), portas `FolderLocator`/`LegacyPathSource`, fim das variáveis `PRAXISFORGE_FOLDER_*`, `add/update --path`, bootstrap `<raiz>__<sub>` (sufixo `_N`, truncamento 63, prefixo `p`) idempotente por caminho, `folders migrate [--root]`.
- 429 testes, cobertura 96,58%; lint, bandit/safety, validate-data e guarda de camadas limpos; quickstart §1–§6 manual ok.
- Branch `005-caminho-absoluto-registro`, 6 commits (`fef2808` … `c891d5d`), sem push.

### 🧭 Decisões

- `src/data/folders.yaml` zerado (v2 vazio) a pedido do usuário — ele fará novo bootstrap/scan (o local tinha 55 pastas, 18 com `status: ignored` inválido).
- `FolderPathNotConfiguredError` removida (órfã); detecção de aliases duplicados do scan ficou inalcançável → anotada no TODO.

### 🐞 Bug achado

- `update_folder` (feature 004) regravava `last_curated_commit` ao editar licença/caminho de pasta já curada; corrigido para gravar só em `--status curated` (docs/bugs/2026-09-23-update-regravava-versao-curada.md).

### ⏭️ Próximo

- Push + PR da 005; usuário refaz `folders bootstrap ~/DevOps/github_forks`.
