---
data: 2026-08-11
dia: Terça-feira
tags: [daily, conky, desktop, hardware, obsidian, mcp]
tipo: nota-diaria
---

# 📅 2026-08-11 — Terça-feira

## 🎯 Resumo do dia

Sessão de ajuste da configuração do Conky (monitor de sistema no desktop): adição de campos de hardware (CPU, GPU, placa-mãe) e remoção da seção de bateria (máquina é desktop, não notebook).

---

## ✅ Concluído

### Conky — novos campos de hardware
- Adicionado ao `conky.text`: **CPU Model**, **GPU Model**, **Board** (fabricante + modelo da placa-mãe)
- Fontes de dados escolhidas (sem necessidade de root/dmidecode):
  - CPU: `grep -m1 "model name" /proc/cpuinfo`
  - GPU: `glxinfo | awk -F': ' '/OpenGL renderer string/{print $2}'` (trocado de `lspci` porque o nome via PCI ID saía genérico; `glxinfo` retorna string limpa: `AMD Radeon RX 580 2048SP`)
  - Placa-mãe: `/sys/devices/virtual/dmi/id/board_vendor` + `board_name` (legível sem sudo, ao contrário de `dmidecode`)
  - Todos com `execi 86400` (dados estáticos, não mudam em runtime)

### Conky — remoção da seção de bateria
- Removidas linhas `battery_bar`, `battery_percent BAT0`, `acpiacadapter`, `battery BAT1` — máquina é desktop, sem bateria

### Correção de bug no arquivo de config
- `~/Documentos/DevOps/conf/conky/conky.conf` continha um wrapper `sudo cat > /etc/conky/conky.conf <<EOF ... EOF` para "auto-instalar" a config
- **Bug 1:** arquivo com terminações de linha CRLF (`\r\n`) — o `EOF` final virava `EOF\r`, o heredoc bash nunca fechava
- **Bug 2 (causa raiz):** mesmo corrigido o CRLF, o heredoc ainda falhava ao ser executado via `sudo bash arquivo.conf` no ambiente não-interativo — o conteúdo da config era interpretado linha a linha como comandos shell (`${color orange}` → erro "bad substitution" / "substituição incorreta")
- **Decisão:** abandonar o padrão heredoc auto-instalável; o arquivo agora é uma config Lua pura do Conky (sem wrapper), instalada via `sudo cp` direto

### Comando de instalação/aplicação validado
```bash
sudo cp /home/yves_marinho/Documentos/DevOps/conf/conky/conky.conf /etc/conky/conky.conf \
  && killall conky && conky --daemonize --pause=1
```

### Conky — rodada de correções pós-aplicação
- **GPU genérica:** fonte trocada de `lspci` para `glxinfo | grep "OpenGL renderer"` (nome mais legível)
- **IP local não aparecia:** config referenciava interface inexistente `enp37s0`; corrigido para `enp5s0` (interface real desta máquina, `ip -o -4 addr show`)
- **Erro `Can't get value of subfeature temp4_min: I/O error`:** bug do driver `gigabyte_wmi` (placa Gigabyte), não do Conky — `sensors` sempre imprime esse erro no stderr mesmo com os dados corretos; fix com `2>/dev/null` nas duas chamadas de `sensors`. Confirmado que `CPU Temp` já usa `Tctl` (temperatura do soquete, não por núcleo)
- **Processos duplicados:** `killall conky` não matou todas as instâncias em uma rodada — um PID antigo continuou rodando com a config anterior (sem o `2>/dev/null`), mascarando o fix. Resolvido com `pkill -9 -x conky`
- **IP Externo em IPv6:** `wget -O- http://ipecho.net/plain` retornava IPv6 (rota IPv6 preferencial da máquina); fix com `wget -4` para forçar IPv4

### Conky — uso de CPU/GPU e status do Ollama
- Adicionado **CPU Uso** (`${cpu}%`, builtin do Conky) e **GPU Uso** (`gpu_busy_percent` via sysfs, `/sys/class/drm/card1/device/gpu_busy_percent` — específico de GPU AMD/amdgpu)
- Adicionado **Ollama** (ativo/inativo) via `systemctl is-active ollama`
- Removidas as linhas `Processes` e `Running` (`${processes}`/`${running_processes}`) a pedido do usuário

### MCP `obsidian-rest` — troca de chave de API e limitação de porta
- Usuário forneceu duas API keys: uma do vault `claude_memory` (porta 27125) e uma do vault `Worldmind` (porta padrão 27124)
- Testado apontar o MCP para `claude_memory`: falhou persistentemente com `Authorization required` mesmo com a chave correta
- **Causa raiz identificada no código-fonte do pacote `mcp-obsidian`** (checado até a v0.2.2 no PyPI): a porta HTTPS `27124` está fixa no código (`obsidian.py`, `port: int = 27124`); não existe suporte a `OBSIDIAN_PORT`/`OBSIDIAN_HOST` via variável de ambiente — só `OBSIDIAN_API_KEY` é lido do ambiente
- **Decisão:** MCP `obsidian-rest` (`~/.claude.json`) mantido configurado com a chave do **Worldmind** (porta padrão 27124) para uso futuro; acesso ao vault `claude_memory` continua via filesystem direto, não via MCP
- Fato registrado em [[../memory/infra-stack|Infra Stack]] para não ser re-descoberto em sessão futura
- Reconexão do MCP após troca de env var requer `/mcp` (comando do usuário) — o processo stdio só lê env vars na inicialização

---

## 🔍 Descobertas técnicas

- `lscpu` está em locale pt-BR (`Nome do modelo:`), então `/proc/cpuinfo` (sempre em inglês, `model name`) é a fonte estável para automação
- `lspci` só reporta o nome do chip GPU (ex.: "Polaris 20 XL [Radeon RX 580 2048SP]"), nunca o fabricante da placa (AIB partner, ex. Sapphire/ASUS) — essa informação não existe no PCI ID
- `glxinfo | grep "OpenGL renderer"` dá o nome de GPU mais limpo e amigável que `lspci`
- `/sys/devices/virtual/dmi/id/board_vendor` e `board_name` são legíveis por usuário comum; `dmidecode` exige root e falha silenciosamente sem sudo (`Permission denied` em `/sys/firmware/dmi/tables/smbios_entry_point`)
- Sudo interativo (pedir senha) não funciona em comandos disparados pelo assistente — sempre pedir ao usuário para rodar via `! <comando>` no terminal dele
- `killall conky` pode deixar processos órfãos vivos; conferir sempre com `pgrep -fa conky` após reiniciar
- `sensors` (lm-sensors) pode reportar erros de I/O em subfeatures não suportadas por drivers específicos de placa (ex.: `gigabyte_wmi`) sem que os dados relevantes sejam afetados — normal suprimir com `2>/dev/null`
- Pacote `mcp-obsidian` (PyPI, até v0.2.2) não suporta porta/host customizados via env var — hardcoded para `127.0.0.1:27124`

---

## 📌 Pendente / Próximos passos

- [ ] Nenhum pendente nesta sessão — config aplicada e validada pelo usuário

---

## 🔗 Arquivos modificados

- `~/Documentos/DevOps/conf/conky/conky.conf`
- `/etc/conky/conky.conf` (via `sudo cp`)
- `~/.claude.json` (env `OBSIDIAN_API_KEY` do MCP `obsidian-rest`, alternado entre chaves e revertido para a do Worldmind)
- `~/Documentos/DevOps/claude_memory/claude_memory/.secrets/obsidian_local_rest_api.json` (chave/porta do próprio vault, documentação local)

## 🏷️ Links relacionados

- [[../projects/conky|conky]]
- [[../memory/infra-stack|Infra Stack]]
